3.1 add 2 server to client both host(home) and slave(phone client) sides, support multi client, support weather cloud service with baidu weather
3.3 add new packaged machanism for the conmmunication between server to host client, new package will add start end and slave fd flag, add timeout 6s for send (and rcv)
3.4 add code for client
3.5 add code for client